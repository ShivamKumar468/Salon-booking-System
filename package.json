{
  "name": "salon-booking-workspace",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "turbo": "^2.0.4"
  },
  "devEngines": {
    "packageManager": {
      "name": "npm",
      "version": "11.13.0"
    }
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
